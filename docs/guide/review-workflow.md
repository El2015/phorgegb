# Review Workflow

Each update should follow this sequence:

1. Edit content
2. Run local preview
3. Review in browser
4. Accept changes
5. Commit to git
6. Push to GitHub

## Commands

```bash
npm run docs:dev
git add .
git commit -m "docs: update reviewed content"
git push
