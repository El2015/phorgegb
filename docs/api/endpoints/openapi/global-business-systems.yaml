# Users API

## GET /api/users

Returns a list of users.

### Request

::: code-group

```bash
curl -X GET http://localhost:3000/api/users

fetch('http://localhost:3000/api/users')


[
  {
    "id": 1,
    "name": "Jane Doe",
    "role": "developer"
  }
]


### Preview checkpoint

- endpoint pages render clearly
- code groups display properly
- sidebar includes the new API pages

---

## 2. Add OpenAPI / Swagger Source Files

### Goal
Keep API docs aligned with real backend definitions.

### Structure

```text
openapi/
  global-business-systems.yaml
